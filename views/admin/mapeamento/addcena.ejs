<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mapeamento de Jogos</title>
    <style>
        body {
            background: #f5f6fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
        }
        .container {
            display: flex;
            height: 100vh;
        }
        .sidebar {
            background: #fff;
            width: 270px;
            padding: 0;
            box-shadow: 2px 0 12px rgba(37,99,235,0.08);
            display: flex;
            flex-direction: column;
            align-items: stretch;
            height: 100vh;
        }
        .logo-space {
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 110px;
            border-bottom: 1px solid #eaf0fe;
            margin-bottom: 0;
        }
        .logo-space img {
            height: 48px;
            filter: drop-shadow(0 2px 8px #2563eb22);
        }
        .menu {
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 0;
            margin-top: 24px;
        }
        .menu-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 0 32px;
            height: 48px;
            font-size: 16px;
            font-weight: 500;
            color: #2563eb;
            text-decoration: none;
            border-radius: 8px 0 0 8px;
            transition: background 0.2s, color 0.2s;
            cursor: pointer;
            position: relative;
        }
        .menu-item.active {
            background: #2563eb;
            color: #fff;
            box-shadow: 2px 0 8px #2563eb22;
        }
        .menu-item:not(.active):hover {
            background: #eaf0fe;
            color: #1746a2;
        }
        .menu-icon {
            font-size: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding-top: 40px;
        }
        .main-content h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 40px;
            color: #222;
            letter-spacing: 1px;
        }
        .cena-card {
            background: #fff;
            border: 2px solid #444;
            border-radius: 24px;
            padding: 32px 24px;
            width: 340px;
            margin: 24px auto 0 auto;
            box-shadow: 0 2px 12px rgba(0,0,0,0.04);
            display: flex;
            flex-direction: column;
            gap: 18px;
            align-items: stretch;
        }
        .cena-card label {
            font-size: 16px;
            color: #222;
            font-weight: 500;
            margin-bottom: 8px;
        }
        .cena-card input {
            padding: 10px 14px;
            border: 1px solid #d1d5db;
            border-radius: 8px;
            font-size: 15px;
            background: #fff;
            transition: border-color 0.2s;
            width: 100%;
            box-sizing: border-box;
            margin-bottom: 0;
        }
        .cena-card input:focus {
            border-color: #2563eb;
            outline: none;
            background: #eaf0fe;
        }
        .cena-card .ok-btn {
            background: none;
            border: none;
            font-size: 16px;
            font-weight: 600;
            color: #222;
            cursor: pointer;
            align-self: flex-end;
            margin-top: 8px;
        }
        .cena-card .ok-btn:hover {
            color: #2563eb;
        }
    </style>
</head>
<body>
    <div class="container">
        <%- include('../../partials/sidebar') %>
        <main class="main-content">
            <h1>Mapeamento de Jogos</h1>
            <form class="cena-card" action="/addcenaok" autocomplete="off">
                <label for="nome-cena">Nome da Cena</label>
                <input type="text" id="nome-cena" name="nome-cena" placeholder="Nome da Cena">
                <button type="submit" class="ok-btn">OK</button>
            </form>
        </main>
    </div>
</body>
</html>