<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mapeamento de Jogos</title>
    <style>
        body {
            background: #f5f6fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
        }
        .container {
            display: flex;
            height: 100vh;
        }
        .sidebar {
            background: #fff;
            width: 270px;
            padding: 0;
            box-shadow: 2px 0 12px rgba(37,99,235,0.08);
            display: flex;
            flex-direction: column;
            align-items: stretch;
            height: 100vh;
        }
        .logo-space {
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 110px;
            border-bottom: 1px solid #eaf0fe;
            margin-bottom: 0;
        }
        .logo-space img {
            height: 48px;
            filter: drop-shadow(0 2px 8px #2563eb22);
        }
        .menu {
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 0;
            margin-top: 24px;
        }
        .menu-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 0 32px;
            height: 48px;
            font-size: 16px;
            font-weight: 500;
            color: #2563eb;
            text-decoration: none;
            border-radius: 8px 0 0 8px;
            transition: background 0.2s, color 0.2s;
            cursor: pointer;
            position: relative;
        }
        .menu-item.active {
            background: #2563eb;
            color: #fff;
            box-shadow: 2px 0 8px #2563eb22;
        }
        .menu-item:not(.active):hover {
            background: #eaf0fe;
            color: #1746a2;
        }
        .menu-icon {
            font-size: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding-top: 60px;
        }
        .main-content h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 40px;
            color: #222;
            letter-spacing: 1px;
        }
        .fase-card {
            background: #fff;
            border: 2px solid #444;
            border-radius: 24px;
            padding: 32px 32px 24px 32px;
            width: 340px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.04);
            display: flex;
            flex-direction: column;
            gap: 18px;
            align-items: stretch;
        }
        .fase-card label {
            font-size: 18px;
            color: #222;
            font-weight: 600;
            margin-bottom: 8px;
        }
        .fase-card .input-wrapper {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: stretch;
        }
        .fase-card input[type="text"] {
            padding: 12px 16px;
            border: none;
            border-radius: 10px;
            font-size: 17px;
            background: #f7faff;
            box-shadow: 0 2px 8px #2563eb11;
            margin-bottom: 0;
            width: 100%;
            box-sizing: border-box;
        }
        .fase-card .ok-btn {
            background: none;
            border: none;
            font-size: 18px;
            font-weight: 700;
            color: #222;
            cursor: pointer;
            align-self: flex-end;
            margin-top: 18px;
            margin-bottom: 4px;
            transition: color 0.2s;
        }
        .fase-card .ok-btn:hover {
            color: #2563eb;
        }
    </style>
</head>
<body>
    <div class="container">
        <%- include('../../partials/sidebar') %>
        <main class="main-content">
            <h1>Mapeamento de Jogos</h1>
            <form class="fase-card" action="/addfaseok" method="GET" autocomplete="off">
                <label for="nome-fase">Nome da fase</label>
                <div class="input-wrapper">
                    <input type="text" id="nome-fase" name="nome-fase" placeholder="Nome da fase">
                </div>
                <button type="submit" class="ok-btn">OK</button>
            </form>
        </main>
    </div>
</body>
</html>