<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrar-se - Ludus</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
    <div class="register-wrapper">
        <div class="logo-space">
            <img src="/logo-ludus.png" alt="Ludus Logo">
        </div>
        <% if (typeof error !== 'undefined') { %>
            <div class="error-message"><%= error %></div>
        <% } %>
        <form class="register-form" action="/registrar" method="POST">
            <div class="form-group">
                <label for="nome_usuario">Nome de Usuário</label>
                <input type="text" id="nome_usuario" name="nome_usuario" placeholder="Digite seu Nome" required>
            </div>
            <div class="form-group">
                <label for="email_usuario">Email</label>
                <input type="text" id="email_usuario" name="email_usuario" placeholder="Digite seu Email" required>
            </div>
            <div class="form-group">
                <label for="senha_usuario">Senha</label>
                <input type="password" id="senha_usuario" name="senha_usuario" required>
            </div>
            <div class="form-group">
                <label for="instituicao_usuario">Instituição</label>
                <input type="text" id="instituicao_usuario" name="instituicao_usuario" placeholder="Digite sua Instituição" required>
            </div>
            <div class="profile-options">
                <label>
                    <input type="checkbox" name="perfil" value="professor">
                    Professor
                </label>
                <label>
                    <input type="checkbox" name="perfil" value="pesquisador">
                    Pesquisador
                </label>
            </div>
            <button type="submit" class="btn-register">ENTRAR</button>
        </form>
        <a href="/" class="back-link">Voltar para login</a>
    </div>
</body>
</html>